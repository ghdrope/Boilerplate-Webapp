frontend:
  image:
    tag: v0.0.1
  replicaCount: 2
  ingress:
    enabled: false
    hosts:
      - host: frontend.staging.mycompany.com
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: frontend-staging-tls
        hosts:
          - frontend.staging.mycompany.com

backend:
  image:
    tag: v0.0.1
  replicaCount: 2

grpc:
  image:
    tag: v0.0.1
  replicaCount: 2
